# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
results.xml

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS / WSL metadata
.DS_Store
Thumbs.db
*Zone.Identifier

# Logs
logs/
*.log

# Data — backtest parquet cache and generated DB only; data/feeds/ source is tracked
data/backtest/
data/db/*.sqlite
data/db/*.sqlite3
*.csv

# Config — optimizer output is generated, not source-controlled by default
config/best_params_*.json

# Secrets — .env contains secrets; .env.example is a safe template and IS tracked
.env

# Docker local override
docker-compose.override.yml

# Accidental pip install artifacts (e.g. mistyped `pip install pkg>=x.y.z`)
=*

# Claude Code — local AI assistant context, not part of the repo
CLAUDE.md
.claude/

# Legacy one-shot patch scripts (already absorbed into source files)
patch_*.py
